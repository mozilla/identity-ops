#!/bin/bash

exec 2>&1 # fix stderr logging for daemontools

export PATH=/usr/local/bin:$PATH
export NODE_ENV=production
export CONFIG_FILES="/opt/certifier/config/production.json"

cd /opt/certifier
ulimit -n 8192
exec setuidgid browserid bin/certifier

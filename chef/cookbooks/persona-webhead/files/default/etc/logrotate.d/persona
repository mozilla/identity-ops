/var/browserid/log/*.log /var/browserid/log/*.json {
    daily
      # default : weekly
    rotate 2
      # default : 4
    missingok
      # default : nomissingok
    notifempty
      # default : ifempty
    copytruncate
}

#!/bin/bash

if scp -o StrictHostKeyChecking=no /opt/bid_metrics/queue/* <%= node[:persona][:webhead][:metrics][:server][node[:aws_region]] %>:/opt/bid_metrics/incoming/; then
  # scp push was successful
  rm -v -f /opt/bid_metrics/queue/*
fi

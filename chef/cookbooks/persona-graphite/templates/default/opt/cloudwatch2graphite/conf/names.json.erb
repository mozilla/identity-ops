[
<% names = node[:stack][:load_balancers].keys %>
<% names.each_index do |x| %>
<%   stack = names[x].split('-')[-1] %>
  {"aws_name": "<%= node[:stack][:load_balancers][names[x]][:name] %>", 
   "graphite_name": "<%= stack + "." + names[x] %>"}<%= x >= names.length - 1 ? "" : "," %>
<% end %>
]
